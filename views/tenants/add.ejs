<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Tenant for Unit <%= unit.unit_no %></title>
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet" />
  </head>
  <body>
    <div class="container mt-5">
      <h1 class="mb-4 text-center">
        Add Tenant for Unit <%= unit.unit_no %> (Building: <%= unit.bldg_name
        %>)
      </h1>

      <form action="/units/<%= unit.unit_id %>/tenants" method="POST">
        <div class="mb-3">
          <label for="first_name" class="form-label">First Name:</label>
          <input
            type="text"
            class="form-control"
            id="first_name"
            name="first_name"
            placeholder="Enter first name"
            required
          />
        </div>

        <div class="mb-3">
          <label for="last_name" class="form-label">Last Name:</label>
          <input
            type="text"
            class="form-control"
            id="last_name"
            name="last_name"
            placeholder="Enter last name"
            required
          />
        </div>

        <button type="submit" class="btn btn-success">Add Tenant</button>
        <a
          href="/units/<%= unit.unit_id %>/tenants"
          class="btn btn-secondary ms-2"
          >Cancel</a
        >
      </form>

      <div class="mt-4 text-center">
        <a href="/units/<%= unit.unit_id %>/tenants"
          >Back to Tenants List for Unit <%= unit.unit_no %></a
        >
      </div>
    </div>
  </body>
</html>
