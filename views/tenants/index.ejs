<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tenant Management</title>
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet" />
    <style>
      .btn-billing {
        background-color: #20c997;
        border-color: #20c997;
        color: white;
      }
      .btn-billing:hover {
        background-color: #17a2b8;
        border-color: #17a2b8;
      }
    </style>
  </head>
  <body>
    <%- include('../partials/navbar') %>

    <div class="container mt-5">
      <h1 class="mb-4 text-center"><%= tenants[0].bldg_name %> Building %></h1>

      <div class="d-flex justify-content-between mb-3">
        <a href="/" class="btn btn-secondary">Back to Home</a>
        <a href="/tenants/add" class="btn btn-success">Add New Tenant</a>
      </div>

      <% if (tenants.length > 0) { %>
      <div class="table-responsive">
        <table class="table table-striped table-hover align-middle">
          <thead class="table-dark">
            <tr>
              <th scope="col">Tenant Name</th>
              <th scope="col">Unit</th>
              <th scope="col" class="text-center">Actions</th>
            </tr>
          </thead>
          <tbody>
            <% tenants.forEach(tenant => { %>
            <tr>
              <td><%= tenant.first_name %> <%= tenant.last_name %></td>
              <td>Unit <%= tenant.unit_no %></td>
              <td class="text-center text-nowrap">
                <a
                  href="/tenants/edit/<%= tenant.tenant_id %>"
                  class="btn btn-sm btn-primary"
                  >Edit</a
                >
              </td>
            </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
      <% } else { %>
      <div class="alert alert-info text-center" role="alert">
        No tenants now.
        <a href="/tenants/add" class="alert-link">Add one now!</a>
      </div>
      <% } %>
    </div>

    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
