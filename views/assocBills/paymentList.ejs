<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Payment List</title>
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet" />
  </head>
  <body>
    <%- include('../partials/navbar') %>

    <div class="container mt-5">
      <h1 class="mb-4 text-center">
        Payment History for Unit <%= unit[0].unit_no %>
      </h1>

      <div class="d-flex justify-content-between mb-3">
        <button class="btn btn-secondary" onclick="history.back()">Back</button>
        <div>
          <a
            href="/assocBills/createPayment/<%= unit[0].assoc_id %>"
            class="btn btn-primary me-2"
            >Create Payment</a
          >
        </div>
      </div>

      <!-- AssocDues Payment History Section -->
      <% if (assocDues.length > 0) { %>
      <div class="table-responsive">
        <table class="table table-striped table-hover">
          <thead class="table-light">
            <tr>
              <th>Ack No.</th>
              <th>Amount Paid</th>
              <th>Date Paid</th>
            </tr>
          </thead>
          <tbody>
            <% assocDues.forEach(bill => { %>
            <tr>
              <td><%= bill.ack_no %></td>
              <td>
                <%= parseFloat(bill.amt_paid).toLocaleString('en-PH', {
                minimumFractionDigits: 2, maximumFractionDigits: 2 }) %>
              </td>
              <td><%= new Date(bill.date_paid).toLocaleDateString() %></td>
            </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
      <% } else { %>
      <div class="alert alert-info text-center" role="alert">
        No Payment History records found.
      </div>
      <% } %>
    </div>

    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
