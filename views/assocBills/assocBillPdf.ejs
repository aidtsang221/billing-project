<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Association Dues Bill PDF</title>
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet" />
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0.5in 1in;
        font-size: 10pt;
        color: #333;
      }
      .container {
        max-width: 700px;
        margin: 0 auto;
      }
      h1,
      h2,
      h3,
      h4,
      h5,
      h6 {
        color: #2c3e50;
        margin-top: 1.5em;
        margin-bottom: 0.5em;
      }
      .header,
      .footer {
        text-align: center;
        margin-bottom: 1.5em;
        border-bottom: 1px solid #eee;
        padding-bottom: 1em;
      }
      .footer {
        margin-top: 2em;
        border-top: 1px solid #eee;
        border-bottom: none;
        padding-top: 1em;
        font-size: 8pt;
        color: #777;
      }
      .bill-details p {
        margin-bottom: 0.3em;
      }
      .summary-table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 1.5em;
      }
      .summary-table th,
      .summary-table td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: left;
      }
      .summary-table th {
        background-color: #f2f2f2;
      }
      .total-amount {
        font-size: 1.2em;
        font-weight: bold;
        text-align: right;
        margin-top: 1em;
      }
      .text-muted {
        color: #6c757d !important;
      }
      .banner {
        width: 100%;
        margin: 0;
        padding: 0;
      }

      .banner .logo {
        width: 100%;
        height: 120px;
        object-fit: cover;
        display: block;
      }
    </style>
  </head>
  <body>
    <div class="banner">
      <img
        src="http://localhost:3000/images/logo.jpg"
        alt="Royal Peak Logo"
        class="logo"
      />
    </div>

    <div class="container">
      <div class="header">
        <h2>Association Dues Bill</h2>
        <!-- <h3><%= assoc.bldg_name %></h3> -->
        <p>ASSOC<%= assoc.assoc_id %></p>
        <p><strong>Unit <%= assoc.unit_no %></strong></p>
        <p><strong>Unit Area</strong>: <%= assoc.unit_area %> sqm</p>
        <p>
          <strong>Owner</strong>: <%= assoc.first_name %> <%= assoc.last_name %>
        </p>
        <p><strong>Rate</strong>: ₱ <%= assoc.amount %> / sqm</p>
      </div>

      <div class="bill-details mb-4">
        <p>
          <strong>Bill Period:</strong> <%= new
          Date(assoc.start_date).toLocaleDateString('en-US', { year: 'numeric',
          month: 'long', day: 'numeric' }) %> to <%= new
          Date(assoc.end_date).toLocaleDateString('en-US', { year: 'numeric',
          month: 'long', day: 'numeric' }) %>
        </p>
        <p>
          <strong>Due Date:</strong> <%= new
          Date(assoc.due_date).toLocaleDateString('en-US', { year: 'numeric',
          month: 'long', day: 'numeric' }) %>
        </p>
      </div>

      <h4 class="mt-4 mb-3">Charges Breakdown</h4>
      <table class="summary-table">
        <thead>
          <tr>
            <th>Description</th>
            <th>Amount</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Association Dues</td>
            <td>
              ₱ <%= parseFloat(assoc.amount *
              assoc.unit_area).toLocaleString('en-US', { minimumFractionDigits:
              2, maximumFractionDigits: 2 }) %>
            </td>
          </tr>
          <tr>
            <td>Adjustment Fee</td>
            <td>
              ₱ <%= parseFloat(assoc.adjustment).toLocaleString('en-US', {
              minimumFractionDigits: 2, maximumFractionDigits: 2 }) %>
            </td>
          </tr>
        </tbody>
      </table>

      <div class="total-amount mt-4">
        Total Amount Due:
        <span style="font-size: 1.5em"
          >₱<%= parseFloat(assoc.total_amt).toLocaleString('en-US', {
          minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></span
        >
      </div>

      <div class="footer">
        <p><%= new Date().toLocaleString() %></p>
      </div>
    </div>

    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
